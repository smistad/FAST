fast_add_sources(
        SegmentationVolumeReconstructor.cpp
        SegmentationVolumeReconstructor.hpp
)
if(FAST_MODULE_NeuralNetwork)
    fast_add_test_sources(
            Tests.cpp
    )
endif()